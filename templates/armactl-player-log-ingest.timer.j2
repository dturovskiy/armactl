[Unit]
Description=Run armactl player-log ingest after each completed cycle ({{ instance }})

[Timer]
OnActiveSec={{ interval_seconds }}s
OnUnitInactiveSec={{ interval_seconds }}s
AccuracySec=1s
RandomizedDelaySec=0
Unit={{ service_name }}

[Install]
WantedBy=timers.target
